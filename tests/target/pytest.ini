[pytest]
addopts = -ra
markers =
    bench: requires an ESP32-S3 bench device running the test-hook build
    physical_fixture: requires external wiring/actuation that pytest cannot provide by itself
